# Changelog

## 2.1.3

- 主动发言不再要求提问或以问句结尾，允许自然表达感受、观察、见闻、小事、感叹和联想。
- 旧配置中的自定义生成提示也会追加新的表达策略，避免升级后仍被历史提示强制追问。
- 现实经历仍须来自 Persona、动态状态或可信上下文；没有依据时只能表达为联想、假设或感受。

## 2.1.1

- 为调度器、桥接层、BotMesh 集成入口和派发结果增加统一 `trace_id` 调用链日志。
- 日志明确输出 Bot/群身份、Persona 作用域与指纹、目标、关系方向、称呼和失败阶段。

## 2.1.0

- BotMesh 模式改为单次调用 BotMesh `dispatch_proactive_topic`，主动话题只保留调度职责。
- Persona、持久化历史、单向关系、称呼、生成、签名与实际发送全部归 BotMesh 管理。
- 只有签名恢复的最新 Bot 身份能成为明确对象；不再根据正文姓名或昵称猜测目标。
- 移除主动话题内的 BotMesh JSON 修复、称呼渲染、展示帧包装与直接发送逻辑。

## 2.0.5

- 修正 BotMesh JSON 输出约束被后续“直接输出正文”提示覆盖的问题。
- 自由文本、多人点名或正文自带称呼时自动低温改写为单目标 JSON；连续失败时使用无称呼群话题兜底。

## 2.0.4

- 取消因正文出现参与者名称而直接判定测试失败的行为。

## 2.0.3

- 复用 BotMesh 正常回复的身份、群 Persona 和单向关系上下文。
- 由发送层根据结构化 `target_id` 确定性添加称呼。
